# go2rtc config — streams registered dynamically at runtime via REST API.
# Run entrypoint.py which calls PUT /api/streams/camera_{mac} per discovered device.

streams: {}

rtsp:
  listen: ":8554"
  default_query: "video"

webrtc:
  listen: ":8555"

api:
  listen: ":1984"

log:
  level: info
